DISCORD_TOKEN=
DASHBOARD_PORT=8080
# Optional: if set, this secret is required on the first-run admin setup screen.
# Keep the real value only in your hosting environment, never in GitHub.
DASHBOARD_SETUP_KEY=
