DISCORD_TOKEN=
DASHBOARD_PORT=8080
# Optional shared secret used only when creating the first Admin account.
# Keep the real value only in your hosting environment, never in GitHub.
DASHBOARD_ADMIN_SECRET=
